
if SHIELD_TOTEM_LEFT

    config ZMK_KEYBOARD_NAME
        default "TOTEM"

    config ZMK_SPLIT_ROLE_CENTRAL
        default y

endif

if SHIELD_TOTEM_LEFT || SHIELD_TOTEM_RIGHT

    config ZMK_SPLIT
        default y

    config ZMK_SPLIT_WIRED
        default y

    # config CONFIG_ZMK_SPLIT_WIRED_UART_MODE_ASYNC
    #     default y
endif
